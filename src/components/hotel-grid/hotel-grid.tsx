import React from "react";
import { Hotel } from "@/src/types/hotel";
import HotelCard from "../hotel-card/hotel-card";

const HotelGrid = (hotel: Hotel) => {
  return <div className="results-grid"></div>;
};

export default HotelGrid;
